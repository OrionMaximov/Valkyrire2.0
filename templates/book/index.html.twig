{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1 class="mt-3 mb-4">livres</h1>
    <div class="row">
        {% for livres in livres %}
            <div class="col-md-4">
                <div class="card mb-4">
                    <img src="{{asset('/uploads/upload/' ~ item.vars.data.livres.image)}}" alt="{{ livres.titre }}" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">{{ livres.titre }}</h5>
                        
                        <div class="d-flex justify-content-between align-item-center">
                            <a href="{{ path('livres.detail', {id: livres.id})}} " class="btn btn-dark">View details</a>
                            <span class="h5 mt-auto">{{ livres.prix }} €</span>
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}